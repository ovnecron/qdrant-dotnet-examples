# Local Run

Start the local stack with `dotnet run --project src/AppHost`.

The API is available on `http://localhost:5010`.

Check `/health` and `/ready` after startup to verify that the local stack is healthy.
