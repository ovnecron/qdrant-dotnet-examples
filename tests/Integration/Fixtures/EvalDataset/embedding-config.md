# Embedding Configuration

The default embedding provider is `Deterministic`.

The default embedding model is `hashing-text-v1`.

The default embedding dimension is `384`.

Vectors store the embedding schema version `v1`.
